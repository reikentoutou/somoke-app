---
description: 云函数 api（Node + wx-server-sdk）实现约定
globs: cloudfunctions/**/*
alwaysApply: false
---

# 云函数后端规则（Somoke Ledger）

- **范围**：只改 `cloudfunctions/api/`（及按需更新 `docs/API_CLOUD.md`）。
- **SDK**：`wx-server-sdk`；`cloud.getWXContext().OPENID` 鉴权；返回 `{ code, msg, data }`。
- **路由**：`event.action`；与 `miniprogram/utils/request.js` 中 `ACTION_MAP` 可对照。
- **数据库**：云开发文档库；集合与权限见 `docs/CLOUD_SETUP.md`。
- **部署**：改依赖后须在开发者工具 **Upload and Deploy: Cloud install dependencies**。
