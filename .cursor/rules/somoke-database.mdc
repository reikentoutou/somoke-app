---
description: 数据库与迁移（schema、migrations）约定
globs: database/**/*
alwaysApply: false
---

# 数据库会话规则（Somoke Ledger）

- **范围**：只改 `database/`（`schema.sql`、`migrations/*.sql`）。不要改 `api/`、`miniprogram/`。
- **引擎与字符集**：InnoDB + utf8mb4；字段、表须有中文 `COMMENT`。
- **变更方式**：对已有库的修改必须新增 `database/migrations/` 下序号递增的 SQL；全量定义与 `schema.sql` 保持一致（新表/新字段同步进 `schema.sql`）。
- **安全**：SQL 与注释中不要写真实数据库密码、主机面板账号；可用占位名如 `your_database_name`。
- **协作**：新字段/新表在回复中用简短列表说明名称、类型、含义，便于 API 实现。
